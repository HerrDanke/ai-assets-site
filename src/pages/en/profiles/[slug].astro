---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const lang = 'en';

export async function getStaticPaths() {
  const profiles = await getCollection('profilesEn');
  return profiles.map(p => ({
    params: { slug: p.slug },
    props: { entry: p },
  }));
}

const { entry } = Astro.props;
const { Content, headings, remarkPluginFrontmatter } = await entry.render();
const { data } = entry;
---

<BaseLayout title={data.title} description={data.description}>
  <div class="detail-layout">
    <article class="detail-content">
      <h1 class="section-title">{data.title}</h1>
      <p class="section-sub">{data.description}</p>
      <div class="markdown-body">
        <Content />
      </div>
    </article>
    <aside class="detail-sidebar">
      <div class="sidebar-card">
        <h4>Metadata</h4>
        <dl>
          <dt>Version</dt>
          <dd>v{data.version}</dd>
          <dt>Type</dt>
          <dd>profile</dd>
          {data.updatedAt && <>
            <dt>Updated</dt>
            <dd>{String(data.updatedAt).slice(0, 10)}</dd>
          </>}
        </dl>
      </div>

      {data.tags && data.tags.length > 0 && (
        <div class="sidebar-card">
          <h4>Tags</h4>
          <div class="card-meta">
            {data.tags.map(tag => (
              <span class="tag">{tag}</span>
            ))}
          </div>
        </div>
      )}

      {data.traits && data.traits.length > 0 && (
        <div class="sidebar-card">
          <h4>Traits</h4>
          <ul class="sidebar-list">
            {data.traits.map(trait => (
              <li>{trait}</li>
            ))}
          </ul>
        </div>
      )}

      {data.thinkingStyle && data.thinkingStyle.length > 0 && (
        <div class="sidebar-card">
          <h4>Thinking Style</h4>
          <ul class="sidebar-list">
            {data.thinkingStyle.map(style => (
              <li>{style}</li>
            ))}
          </ul>
        </div>
      )}

      {data.applicableScenarios && data.applicableScenarios.length > 0 && (
        <div class="sidebar-card">
          <h4>Applicable Scenarios</h4>
          <ul class="sidebar-list">
            {data.applicableScenarios.map(scene => (
              <li>{scene}</li>
            ))}
          </ul>
        </div>
      )}

      {data.usedBy && data.usedBy.length > 0 && (
        <div class="sidebar-card">
          <h4>Used By</h4>
          <ul class="sidebar-list">
            {data.usedBy.map(agent => (
              <li><a href={`/en/agents/${agent}/`}>{agent}</a></li>
            ))}
          </ul>
        </div>
      )}

      <div class="sidebar-card sidebar-actions">
        <button class="asset-card-btn" data-slug={entry.slug} data-kind="profiles" data-action="copy" title="Copy Markdown" style="width: 100%; height: auto; padding: 8px;">📋 Copy Markdown</button>
        <button class="asset-card-btn" data-slug={entry.slug} data-kind="profiles" data-action="download" title="Download .md" style="width: 100%; height: auto; padding: 8px;">⬇ Download .md</button>
      </div>
    </aside>
  </div>
</BaseLayout>

<style>
  .detail-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
  }
  .detail-content { min-width: 0; }
  .markdown-body {
    margin-top: 24px;
    line-height: 1.8;
    font-size: 15px;
  }
  .markdown-body :global(h2) {
    font-size: 20px;
    margin: 32px 0 12px;
    font-weight: 600;
  }
  .markdown-body :global(h3) {
    font-size: 17px;
    margin: 24px 0 10px;
    font-weight: 600;
  }
  .markdown-body :global(p) {
    margin-bottom: 16px;
    color: var(--text-dim);
  }
  .markdown-body :global(ul), .markdown-body :global(ol) {
    margin: 12px 0 16px;
    padding-left: 24px;
    color: var(--text-dim);
  }
  .markdown-body :global(li) {
    margin-bottom: 6px;
  }
  .markdown-body :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 16px 0;
    font-size: 14px;
  }
  .markdown-body :global(th), .markdown-body :global(td) {
    padding: 10px 12px;
    border: 1px solid var(--border);
    text-align: left;
  }
  .markdown-body :global(th) {
    background: var(--bg-elevated);
    font-weight: 600;
  }
  .markdown-body :global(code) {
    background: var(--bg-elevated);
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 13px;
  }
  .markdown-body :global(pre) {
    background: var(--bg-soft);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 16px;
    overflow-x: auto;
  }
  .mermaid :global(svg) {
    max-width: 100% !important;
    height: auto !important;
    margin: 16px 0;
  }
  .markdown-body :global(pre code) {
    background: none;
    padding: 0;
  }
  .detail-sidebar {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .sidebar-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 16px;
  }
  .sidebar-card h4 {
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 12px;
    color: var(--text);
  }
  .sidebar-card dl {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 6px 12px;
    font-size: 13px;
  }
  .sidebar-card dt {
    color: var(--text-faint);
  }
  .sidebar-card dd {
    color: var(--text);
    margin: 0;
  }
  .sidebar-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .sidebar-list li {
    margin-bottom: 6px;
    font-size: 13px;
    color: var(--text-dim);
  }
  .sidebar-list a {
    color: var(--accent-2);
    transition: color 0.2s;
  }
  .sidebar-list a:hover {
    color: var(--accent);
  }
  .card-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }
  .tag {
    font-size: 11px;
    padding: 2px 8px;
    border-radius: 99px;
    background: var(--bg-elevated);
    color: var(--text-dim);
    border: 1px solid var(--border-soft);
  }
  .sidebar-actions {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  @media@media@media (max-width: 480px) {
    .markdown-body { font-size: 14px; }
    .markdown-body :global(h2) { font-size: 18px; }
    .markdown-body :global(h3) { font-size: 15px; }
    .sidebar-card { padding: 12px; }
  }
</style>
